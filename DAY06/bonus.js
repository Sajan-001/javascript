// Simple Login Checker

let username = "admin";
let password = "1234";

if (username === "admin" && password === "1234") { // && checks both
    console.log("Login Successful");
} else {
    console.log("Invalid Credentials");
}